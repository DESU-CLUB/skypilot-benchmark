{
  "task_name": "configure_sky_resources_yaml",
  "task_description": "You are working in a SkyPilot environment. Write a SkyPilot task YAML file at `/home/user/finetune_job.yaml` that configures a large-scale fine-tuning job.\n\nThe job has these requirements:\n\n1. **Job name**: `llama-finetune`\n2. **Compute**: 4 A100 GPUs on a single node, using spot instances to reduce cost.\n3. **Multi-cloud fallback**: The job should be able to run on AWS (us-east-1) OR GCP (us-central1), whichever is available first.\n4. **Auto-shutdown**: The cluster must automatically stop after being idle for **30 minutes**.\n5. **Environment variable**: Set `MODEL_NAME` to `meta-llama/Llama-3-8B`.\n6. **Working directory**: Set the working directory to `.` (current directory).\n7. **Setup**: Install dependencies from `requirements.txt` using pip.\n8. **Run**: Execute `python finetune.py --model $MODEL_NAME --epochs 3`.\n\nSave the completed configuration to `/home/user/finetune_job.yaml`. The file must be valid YAML that SkyPilot can parse.",
  "truth": "The file `/home/user/finetune_job.yaml` must exist and be valid YAML.\n\nVerification by parsing YAML content:\n- `name` field must be `llama-finetune`.\n- `resources.accelerators` must reference `A100:4`.\n- `resources.use_spot` must be `true`.\n- `resources.any_of` must contain entries for AWS (us-east-1) and GCP (us-central1).\n- `resources.autostop` must be `30`.\n- `envs.MODEL_NAME` must be `meta-llama/Llama-3-8B`.\n- `workdir` must be `.`.\n- `setup` must contain `pip install` referencing `requirements.txt`.\n- `run` must contain `finetune.py` and reference `MODEL_NAME`.\n\nRunning `python3 -c \"import yaml; yaml.safe_load(open('/home/user/finetune_job.yaml'))\"` must succeed."
}
