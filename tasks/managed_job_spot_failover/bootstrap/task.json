{
  "task_name": "managed_job_spot_failover",
  "task_description": "You are working in a SkyPilot environment. Your goal is to configure a **managed job** that is resilient to spot instance preemptions and specific runtime errors.\n\nCreate two files:\n\n**1. SkyPilot managed job YAML at `/home/user/managed_train.yaml`:**\n\nThe job must be configured as follows:\n- Job name: `resilient-train`\n- Compute: 1 A100 GPU on spot instances.\n- Multi-cloud fallback across **three clouds**: AWS (us-east-1), GCP (us-central1), and Azure (eastus).\n- **Job recovery strategy**: Use the `FAILOVER` strategy. The job should retry automatically when preempted.\n- **Error handling**: The job should retry up to **3 times** when it exits with a non-preemption error. Additionally, it should specifically retry on exit codes **33** (NCCL timeout) and **1** (general error).\n- Setup: Install PyTorch.\n- Run: Execute `python train.py`.\n\n**2. Submission script at `/home/user/submit_job.sh`:**\n- Launch the managed job using the `sky jobs launch` command with the YAML file.\n- Use the job name `resilient-train` (pass it explicitly to override any YAML name).\n- Run non-interactively (no confirmation prompts).\n- After launching, check the job queue with `sky jobs queue` and write the output to `/home/user/job_status.log`.\n\nBoth files must exist at the exact paths specified.",
  "truth": "Two files must exist: `/home/user/managed_train.yaml` and `/home/user/submit_job.sh`.\n\nFor `/home/user/managed_train.yaml`:\n- `name` must be `resilient-train`.\n- `resources.use_spot` must be `true`.\n- `resources.accelerators` must reference A100:1.\n- `resources.any_of` must include AWS, GCP, and Azure entries.\n- `resources.job_recovery.strategy` must be `FAILOVER`.\n- `resources.job_recovery.max_restarts_on_errors` must be `3`.\n- `resources.job_recovery.retry_on_return_codes` must contain `33` and `1`.\n- `setup` must install PyTorch.\n- `run` must contain `train.py`.\n\nFor `/home/user/submit_job.sh`:\n- Must contain `sky jobs launch` command referencing `managed_train.yaml`.\n- Must use `-y` or `--yes` for non-interactive mode.\n- Must contain `--name resilient-train` or `--job-name resilient-train`.\n- Must contain `sky jobs queue` command.\n- Must redirect output to `/home/user/job_status.log`.\n- Must be executable."
}
