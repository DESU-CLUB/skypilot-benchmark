{
  "task_name": "skyserve_vllm_autoscaling",
  "task_description": "You are working in a SkyPilot environment. Your goal is to configure and prepare a **vLLM model serving deployment** using SkyPilot's SkyServe feature with autoscaling.\n\nCreate three files:\n\n**1. SkyServe YAML at `/home/user/vllm_serve.yaml`:**\n\nThis YAML must configure a production-grade LLM serving deployment:\n- Service name: `vllm-llama`\n- **Readiness probe**: Check the `/v1/models` HTTP endpoint to determine when a replica is ready to serve traffic. Set an initial delay of **30 seconds**.\n- **Autoscaling policy**:\n  - Minimum replicas: **1** (always keep one warm).\n  - Maximum replicas: **5** (scale out under load).\n  - Target QPS per replica: **10** (scale out when each replica exceeds 10 requests/second).\n  - Upscale delay: **60 seconds**.\n  - Downscale delay: **300 seconds** (5 minutes) to avoid flapping.\n- **Compute**: Each replica gets 1 A100 GPU on AWS, using **on-demand** instances (not spot — serving requires stability).\n- **Environment**: Set `MODEL_NAME` to `meta-llama/Meta-Llama-3-8B-Instruct`.\n- **Setup**: Install `vllm` via pip.\n- **Run**: Start the vLLM OpenAI-compatible server on port **8080**, binding to `0.0.0.0`, using the `$MODEL_NAME` environment variable.\n\n**2. Deployment script at `/home/user/deploy.sh`:**\n- Use `sky serve up` to deploy the service from `vllm_serve.yaml`.\n- Explicitly name the service `vllm-llama`.\n- Run non-interactively.\n- After deploying, check the service status with `sky serve status vllm-llama` and save the output to `/home/user/serve_status.log`.\n\n**3. Service config JSON at `/home/user/serve_config.json`:**\n- Write a JSON file documenting the deployment parameters:\n  - `\"service_name\"`: `\"vllm-llama\"`\n  - `\"model\"`: `\"meta-llama/Meta-Llama-3-8B-Instruct\"`\n  - `\"min_replicas\"`: `1`\n  - `\"max_replicas\"`: `5`\n  - `\"port\"`: `8080`\n\nAll three files must exist at the exact paths specified.",
  "truth": "Three files must exist: `/home/user/vllm_serve.yaml`, `/home/user/deploy.sh`, and `/home/user/serve_config.json`.\n\nFor `/home/user/vllm_serve.yaml`:\n- `service.readiness_probe.path` must be `/v1/models`.\n- `service.readiness_probe.initial_delay_seconds` must be `30`.\n- `service.replica_policy.min_replicas` must be `1`.\n- `service.replica_policy.max_replicas` must be `5`.\n- `service.replica_policy.target_qps_per_replica` must be `10`.\n- `service.replica_policy.upscale_delay_seconds` must be `60`.\n- `service.replica_policy.downscale_delay_seconds` must be `300`.\n- `resources.accelerators` must reference A100:1.\n- `resources.use_spot` must be `false` (on-demand for stability).\n- `envs.MODEL_NAME` must be `meta-llama/Meta-Llama-3-8B-Instruct`.\n- `setup` must install `vllm`.\n- `run` must start vllm server on port 8080 using $MODEL_NAME.\n\nFor `/home/user/deploy.sh`:\n- Must contain `sky serve up` referencing `vllm_serve.yaml`.\n- Must use `--name vllm-llama` or `-n vllm-llama`.\n- Must use non-interactive flag (-y or --yes).\n- Must contain `sky serve status vllm-llama`.\n- Must redirect status output to `/home/user/serve_status.log`.\n- Must be executable.\n\nFor `/home/user/serve_config.json`:\n- Must be valid JSON.\n- Must contain `service_name: vllm-llama`.\n- Must contain `model: meta-llama/Meta-Llama-3-8B-Instruct`.\n- Must contain `min_replicas: 1`, `max_replicas: 5`, `port: 8080`."
}
